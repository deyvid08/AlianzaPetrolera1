@model IEnumerable<AlianzaPetrolera.Models.Admin.Categoria>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Matricula</h1>
@*<button type="button" class="btn btn-warning"> @Html.ActionLink("Crear Categaoria", "Create")</button>*@
<br />
<br />
<br />
<table class="table table-hover table-dark">
    <tr class="bg-warning text-white text-center">
        <th style="border: none;">
            @Html.DisplayNameFor(model => model.Cate_Name)     
        </th>
        <th></th>
    </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Cate_Name)
                        </td>
                        <td>
                            @*@Html.ActionLink("Edit", "Edit", new { id=item.Cate_Id }) |*@
                            @*@Html.ActionLink("Details", "Details", new { id=item.Cate_Id }) |*@
                            <button type="button" class="btn btn-warning text-white">@Html.ActionLink("Matricular", "Delete", new { id = item.Cate_Id }, htmlAttributes: new { @style = "color:#fff; " })</button>
</td>
                    </tr>
                }

            </table>
